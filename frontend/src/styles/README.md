# ZyID styles

The shared matte theme currently lives in `src/index.css` so Vite loads it once at the application entry point. Keep new layout and visual rules here when the stylesheet needs to be split further.