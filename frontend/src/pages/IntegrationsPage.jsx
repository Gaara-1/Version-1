import { useState } from 'react';
import { CheckCircle2, Database, Link2, LockKeyhole } from 'lucide-react';
export function IntegrationsPage({ notify }) {
    const [connected, setConnected] = useState(false);
    const [link, setLink] = useState('');
    return <div className="space-y-7"><div><div className="label-caps">Connections / Browser-only</div><h1 className="mt-2 font-display text-3xl font-extrabold">Import surfaces, not dependencies.</h1><p className="mt-2 max-w-2xl text-sm text-slate-500">The prototype keeps integrations demonstrative. This local build stores no API keys and never contacts DigiLocker or an external verification API.</p></div><div className="data-grid"><div className="span-7 surface rounded-2xl p-5 sm:p-7"><div className="flex items-start gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#e8edf1] text-[#536b80]"><Database size={21}/></div><div><h2 className="font-display text-xl font-extrabold">DigiLocker demo connector</h2><p className="mt-1 text-sm text-slate-500">Simulate a connected document source without an account or network request.</p></div></div>{connected ? <div className="mt-7 rounded-xl border border-[#b8dfc9] bg-[#e8f5ed] p-4"><div className="flex items-center gap-2 text-sm font-bold text-[#1f8a5a]"><CheckCircle2 size={16}/> DigiLocker connected locally</div><p className="mt-1 text-xs text-slate-500">Demo connector ready to hand a local document to Smart Scan.</p><button className="matte-button secondary mt-4" onClick={() => { setConnected(false); notify('Demo connection cleared'); }}>Disconnect</button></div> : <button className="matte-button primary mt-7" onClick={() => { setConnected(true); notify('DigiLocker connected in local demo mode'); }}><Link2 size={15}/> Connect DigiLocker demo</button>}<div className="mt-7 border-t border-slate-200 pt-5"><div className="label-caps">Optional share link</div><div className="mt-3 flex flex-col gap-2 sm:flex-row"><input data-testid="input-digilocker-link" className="field" value={link} onChange={(event) => setLink(event.target.value)} placeholder="Paste a local share reference"/><button className="matte-button secondary" disabled={!link.trim()} onClick={() => notify('Share reference staged for Smart Scan')}>Stage link</button></div></div></div><div className="span-5 surface rounded-2xl p-5"><div className="flex items-center gap-2 text-sm font-bold"><LockKeyhole size={16}/> Connection posture</div><div className="mt-5 space-y-4">{[['No cloud calls', 'Everything runs from the browser session'], ['No API keys', 'Nothing is requested or stored'], ['Local handoff', 'Use Smart Scan to inspect a file']].map(([title, text]) => <div key={title} className="border-l-2 border-[#9eb0bd] pl-3"><div className="text-sm font-bold">{title}</div><p className="mt-1 text-xs leading-relaxed text-slate-500">{text}</p></div>)}</div></div></div></div>;
}
